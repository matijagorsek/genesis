{
  "id": "node-web",
  "name": "Web app with a Node backend",
  "description": "An HTML/JS front end served by a small Node HTTP server with a JSON API and a data file. Runs in the Node toolbox (created on first use), nothing installed into the OS.",
  "entry": "server.js",
  "dev": { "cmd": "genesis-toolbox node -- node server.js --port {port}", "port": 5500, "open": "http://127.0.0.1:{port}/" },
  "run": { "cmd": "genesis-toolbox node -- node server.js --port 5500", "port": 5500, "open": "http://127.0.0.1:5500/" }
}
