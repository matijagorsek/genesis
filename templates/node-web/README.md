# {{name}}

A small Node web app: static files in `public/`, a JSON API in `server.js`, data in `data.json`.
Runs in the Genesis Node toolbox: `genesis-toolbox node -- node server.js --port 5500`. Made with Genesis.
