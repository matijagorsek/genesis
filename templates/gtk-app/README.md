# {{name}}

A desktop window with GTK 4 through Python's GObject bindings, both already in the Genesis image, so it
runs with no install. Items are saved under `~/.local/share/{{name}}/`.

    python3 app.py

Install it to the app menu from the maker ("Install") and it appears like any other app.
