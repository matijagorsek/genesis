# Changelog

Derived from the decision log (`docs/decisions.md`); one line per decision, newest first. Versions are 0.2.N on the stable channel; 0.1.N before 2026-09-15.

## 2026-09-16
- Two templates from issue #2: a JSON API server and a GTK 4 desktop app (built, decision 105)
- Grow-on-boot on x86 actually grows now: a padded partition number from lsblk was refused by growpart (decision 104)
- Verified on 0.2.153 in the VM with captures; MCP calls keep their declared tier (decision 103)
- German and Slovenian for the first-run wizard and Babel's welcome (done, decision 102)
- First evaluation numbers (1 of 3 on the tiny pack); the agent stops a small model that loops on writing (decision 101)
- Prompt templates in chat; printers, wifi diagnosis, power, Bluetooth and audio in the system tools; licence tiers enforced; speculative decoding via a draft model role (built, decisions 97 to 100)
- A reranker on search for packs that ship one (built, decision 95)
- Scanned PDFs and photos are read through the vision model in chat (built, decision 96)
- Routing by task: the maker takes the coder, chat the chat model, both fall back to the small one (done, decision 94)
- A weekly ten-make evaluation on the tiny pack in CI, scored (built, decision 92)
- An Ollama-compatible door on 11434 for apps that speak that API (built, decision 91)
- The plan card before a job: what it will touch and the steps, Proceed / Edit / do it myself (built, decision 90)
- The file index searches by meaning too, with the pack's embedding model (built, decision 89)
- Chat: kept, searchable conversations with documents, photos and the system tools (built, decision 88)
- MCP tool servers as the extension point; the shipped system server makes the maker an administrator (built, decision 87)
- Local completion in Babel from the small model on this machine; debugpy activation fixed (built, decision 86)
- A resized disk is used on the x86 flavours too: growpart in the image (done, decision 85)
- Base images pinned on our own mirror (`ghcr.io/matijagorsek/genesis-base`), moved forward weekly by a bump job (done, decision 82)
- Recipes as a first-class thing in the maker: a Recipes row, one click to replay, save from the gallery (built, decision 83)
- The canary is a gate for the stable tags; German and Slovenian complete for the maker, palette and Settings; screen-reader pass with Orca, every control named (done, decision 84)

## 2026-09-15
- Packs as signed OCI artifacts (built, decision 66)
- Local file index (built, decision 67)
- Ask about the screen (built, decision 68)
- Phone, further (partly, decision 69)
- Security review of the Rust daemons (independent adversarial read of the code, deployment and supply chain) (built, decision 70)
- Fresh-disk run of a downloaded release, as a new user: 0.1.102 arm64 from GitHub, checksums verified, first login, wizard recommends the tiny pack on 8 GB and downloads it, the starter make builds the checklist app, a second make from the launcher builds the pomodoro script with the 2B model on CPU in about four minutes (built, decision 71)
- Security follow-ups from the review, all four: the secrets and system rules match on the canonical path (every spelling of home, symlinks resolved, so a link in the project pointing at a key is a key); a sandboxed shell that asks for network and names a denied host (cloud metadata, loopback services) is refused outright, since a domain filter cannot reach inside a shell; the audit chain is keyed with a per-install secret (HMAC-SHA256, 0600 next to the log), so editing the log without the key breaks the chain; the three base images are pinned by digest with a weekly job that moves the pins forward as a reviewed commit, and the Piper and llama-swap tarballs are verified by checksum before extraction (built, decision 72)
- First real upgrade across the series: the fresh VM installed from 0.1.102 switched to the `stable` channel, pulled 0.2.107 under the signature policy, rebooted into it with projects, models and settings intact, and the new user units (packs, index, phone) came up enabled (built, decision 73)
- First real phone: an iPhone paired with Genesis running in the Mac VM, added by address from the Genesis side since the VM sits behind QEMU's NAT and cannot be discovered; the phone answers back through the forwarded port (verified, decision 74)
- Improvement review (independent read of every surface, the daemons, units, workflows and docs; twenty findings) (built, decision 75)
- Phone app, started (built, decision 76)
- Phone app, second pass, verified on the founder's phone: a quiet foreground service watches the paired Genesis and raises a notification with Allow once / Not now when a job waits for an answer (the answer goes through the same permission API), and one when a job finishes; a "Made" tab lists what Genesis built; the companion daemon honours the default mode from Settings (built, decision 77)
- Babel, the Genesis IDE (built, decision 78)

## 2026-09-14
- Update channel: an installed Genesis follows the image it was installed from (`ghcr.io/matijagorsek/genesis:0.1` or `:0.1-arm64`) (built, decision 52)
- Distribution basics: a download page at https://matijagorsek.github.io/genesis/ (GitHub Pages from docs/) that shows the latest release and the right file per platform; every pushed image is signed with sigstore (cosign, keyless with the workflow's GitHub identity), verifiable with `cosign verify --certificate-identity-regexp github.com/matijagorsek/genesis --certificate-oidc-issuer https://token.actions.githubusercontent.com ghcr.io/matijagorsek/genesis:<tag>` (built, decision 53)
- NVIDIA flavour: `ghcr.io/matijagorsek/genesis:0.1-nvidia`, the same Genesis layer on Aurora's nvidia-open base (open kernel modules), built, self-checked, pushed and signed on every release (built, decision 54)
- Genesis in System Settings: `kcm_genesis`, a QML module (KCMUtils 6) under System Administration, showing updates, the machine, models on disk, the default autonomy, day/night, voice and undo, all read and written through the local Genesis daemon (built, decision 55)
- Small pieces of the "later" list: a python-web template (standard-library server with a JSON API and a front end, verified); KDE Connect allowed through the firewall on the plain-Fedora flavour and mentioned at the end of first run; the first proactive card, a staged update, shown in the dock widget with a green dot and a "restart to apply" line (built, decision 56)
- Proactive cards, first two kinds: a made project whose last run ended with an error ("Fix it" hands the log to the maker) and a made project never installed to the menu ("Install") (built, decision 57)
- Claude, the login way and no API keys: the founder asked for account sign-in rather than keys (built, decision 58)
- Toolchains without touching the OS: `genesis-toolbox node (rust -- cmd` creates a distrobox per language on first use (Fedora + nodejs/npm or cargo/rustc) with the home folder shared; two new templates use it, node-web (Node HTTP server with a JSON API and a front end) and rust-cli (argument parsing and a test). Verified in the arm64 VM: first use creates the Node box in 6 minutes (Node 22) and the Rust box in 4 minutes (cargo 1.98), then commands run directly., decision 59)
- Signature enforcement: images are now signed twice, keyless (GitHub identity, for people) and with a Genesis release key (for machines) (built, decision 60)
- Small models and the maker: a compact mode for the "fast" class (2B-4B): a short strict script (scaffold, read, write the entry file, run, fix, summarise) and nine tools instead of nineteen (built, decision 61)
- The update channel, verified against the real registry after the GHCR package went public: the native VM pulled `ghcr.io/matijagorsek/genesis:0.1-arm64` with `bootc switch` (3 GB, 12 minutes), staged it, and after a reboot runs that CI-built image with the login screen, the model service and the status publisher up (done, decision 62)
- Policy review with an adversarial test battery (24 commands a model might emit) (sh`) was allowed in Hands-off, now it is a system change that always asks. Genesis' own settings and Claude Code's state are secrets. The battery stays in the test suite., decision 63)
- Hygiene and reach for a public project: LICENSE (Apache-2.0) and NOTICE with every redistributed component's licence, CONTRIBUTING, SECURITY, a code of conduct and issue templates including a hardware report (built, decision 64)
- Phone, day one (built, decision 65)

## 2026-09-13
- arm64 flavour built and verified natively on an Apple Silicon Mac: plain Fedora bootc 44 base with Plasma installed by the image, all Genesis pieces, strict lint and a clean self-check (done, decision 46)
- Tuned inference for arm64 and VMs (done, decision 47)
- First maker run inside Genesis itself, on the tiny pack's 2B model in the native VM: 12 turns; it planned, scaffolded, read files, ran the test command, asked permission for a package install exactly as designed, then declared success without implementing the request (done, decision 48)
- Found by installing the cpu pack in the VM: the test disk is 11 GB, the pack 10 GB; the download ran out of space mid-file, and the API let me finish anyway, leaving a system with no router config (done, decision 49)
- Measured the "cpu" pack's 9B code model inside the 16 GB native VM: prompt 6 tok/s, generation 0.9 tok/s on 6-8 vCPUs, so a maker turn with a few thousand tokens of context takes minutes and the job timed out (done, decision 50)
- The promise, end to end, inside Genesis: in the native arm64 VM, the maker took "a checklist app that saves to a file", scaffolded into its own folder, wrote a real CLI with add/done/undone/remove/clear, atomic JSON saves and seven unit tests, found and fixed three of its own bugs (a typo, a shadowed test method, an id-counter bug), ran a demo, and cleaned up: 17 turns, all tests passing (done, decision 51)

## 2026-09-12
- Voice output built: Piper (upstream static Linux build, 26 MB, with its espeak-ng data and onnxruntime) under `/usr/lib/genesis/piper`, an optional `tts` voice (en_US-lessac-medium, 70 MB) in every pack, `POST /api/speak` in agentd returning WAV, and a speaker toggle in the maker that reads replies aloud (built, decision 32)
- Design plan after three research passes (Plasma identity, what wows in 2025–26 products, agent interaction design): identity comes from colour + wallpaper + type + dock + boot-to-login continuity, not replaced widgets; twelve wow moments ranked; ten behaviour rules (one door at Meta+Space, plan card, plain-verb prompts, Keep/Undo, provable "on this machine", voice acknowledges in 300 ms, first run ends with something made) (accepted, decision 33)
- UX pass on the two surfaces people touch, against the ten rules: starter prompts under the maker's field; modes renamed Ask / Trusted / Hands-off with plain descriptions; permission cards say "Genesis wants to install software / use the network / change a file outside the project", with Not now / Allow once (tier codes only in tooltips); allowed decisions no longer clutter the step list; a Keep / Undo toast after each job that touched anything outside the project; the wizard's last step offers three things to make, and genesis-window opens the maker with that prompt the moment the desktop starts (built, decision 34)
- First real desktop session through the dev loop (v0.1.34 in QEMU, SSH, plasmashell scripting over D-Bus) exposed that every image so far shipped no panel: the look-and-feel layout script used `new Desktop()`, which Plasma 6 layout scripting does not have, so it died on line 3 (done, decision 35)
- Select-to-act built and verified live in the VM: Meta+Shift+Space runs `genesis-ask-selection`, which takes the Wayland selection and opens the maker with "About this selection: …" as the request (built, decision 36)
- Genesis Settings, first version: a page in the Genesis window (`/settings`, menu entry under Settings) built from local sources only: hardware and model service state, models on disk, default autonomy mode (stored in ~/.config/genesis/settings.json and picked up by the maker), voice readiness, undo history from the transaction ledger (built, decision 37)
- One door: `Meta+Space` now opens the palette (`genesis-window --palette`, page `/palette` from agentd): one field, hold-to-talk, a chip for the current selection, starter prompts, Enter hands the request to the maker and the window grows into it, Esc closes (built, decision 38)
- Polish for the VM path: voice acknowledges within milliseconds of the press (chime + glow in the palette and the maker, before the microphone even opens; a lower tone on release), and Settings shows the measured speed of the last reply (tokens per second, recorded around every completion) (built, decision 39)
- One-key theme swap: Genesis Light colour scheme (same accent, light neutrals), `genesis-theme dark (light, decision 40)
- Found in a real v0.1.47 session: the user-level permission broker service never started in any image (ProtectSystem=strict with ReadWritePaths to a directory that did not exist yet: exit 226/NAMESPACE) (done, decision 41)
- Terminal: `genesis-ask` (alias `ask`) turns a sentence into one command via the small local model, classifies it with the shared permission policy ("reads only", "changes files here", "changes the system", refused), and runs it only on confirmation; `--print` feeds a bash Ctrl+G binding that rewrites the typed line into the command (built, decision 42)
- Found with a real model download in the VM: the router service crash-looped since the first image because a socket unit held 127.0.0.1:8080 while llama-swap binds the port itself (no socket-activation support) (done, decision 43)
- First real model run inside the VM on this Mac: the wizard downloaded the tiny pack (1.9 GB) over QEMU user networking, the router served it, and a chat completion returned a real reply (18 tokens; 10 minutes wall time on the emulated x86_64 CPU, almost all of it model loading) (done, decision 44)
- Limit found: on the emulated x86_64 VM on Apple Silicon the model service works but is far too slow to use (a 21-token prompt took 10 minutes including load; the `ask` prompt timed out at 10 minutes) (proposed, decision 45)

## 2026-09-11
- llama.cpp ships as the upstream Vulkan release build under `/usr/lib/genesis/llama.cpp`, not Fedora's `llama-cpp` package (months behind, no Vulkan backend, drags the full ROCm stack in, +2.5 GB) (accepted, decision 11)
- First Genesis image built: `genesis:0.1` from `ghcr.io/ublue-os/aurora:stable`, 3.80 GB (base 3.70) (done, decision 12)
- First boot (done, decision 13)
- Release v0.1.10: first fully green pipeline, both jobs (done, decision 14)
- genesis-permd built: first Rust daemon (done, decision 15)
- Release v0.1.14: first image with genesis-permd inside; CI has a Rust test job and diffs the shipped policy against the binary; boot test exercises the broker in the VM (done, decision 16)
- Hostname fixed in v0.1.17 via `systemd.hostname=genesis` in `/usr/lib/bootc/kargs.d` (done, decision 17)
- genesis-probe built: detects GPUs (sysfs, nvidia-smi), RAM, CPU flags, free disk; computes a memory budget (VRAM, unified 75%, or 60% RAM); evaluates the shipped packs' triggers with per-model `optional` and `offload` flags; ranks by platform specificity then trigger tier; writes `/etc/genesis/profile.json` from a first-boot oneshot (done, decision 18)
- Release v0.1.21: first boot writes `/etc/genesis/profile.json` via genesis-probe.service; the CI VM (4 GB, no GPU) gets pack `tiny` (done, decision 19)
- genesis-firstrun built: first-run wizard as a local web UI + JSON API on 127.0.0.1:11510 (root service until `/var/lib/genesis/first-run-done`; user unit opens it at first graphical login) (done, decision 20)
- Release v0.1.22: first-run wizard service active at first boot in the VM; its API answers from inside the booted system (recommended pack `tiny` on the 4 GB CI VM) (done, decision 21)
- genesis-agentd built: tool-calling loop on the local endpoint (shell, read/write/edit file, list dir), every call converted to a permd intent and gated in-process (allow / prompt-and-wait for a client / deny), bubblewrap shell sandbox on Linux, local API on 127.0.0.1:11520 for launchers plus headless `run` (done, decision 22)
- genesis-txd built: transactions with two snapshot backends (btrfs subvolume snapshot when on btrfs as root; exact file pre-images otherwise), a ledger (files, btrfs, flatpak user refs, shell commands marked not undoable), commit and rollback, CLI `list / show / undo` (done, decision 23)
- Gap found by a real run and closed: shell redirects and mutating commands naming absolute or `~` paths outside the project are now W2 (system paths S2), with those paths pre-snapshotted by agentd (done, decision 24)
- Course correction from the founder: the product is "tell the OS what you want made and it makes it", not a chat (done, decision 25)
- Launcher on Plasma: `genesis-krunner`, a KRunner D-Bus runner (`ask …` → "Ask Genesis" → opens the workspace with the prompt), a desktop entry, and a Meta+Space default in /etc/xdg/kglobalshortcutsrc (built, decision 26)
- Found on the v0.1.26 boot capture: the "Welcome to Plasma Desktop / Begin Setup" screen is Plasma Setup (KDE's out-of-box account wizard, run by `plasma-setup.service` until `/etc/plasma-setup-done` exists), not the removed Aurora welcome app; the login manager's wallpaper is `/usr/lib/plasmalogin/defaults.conf` (fixed, verifying, decision 27)
- Clarified with the founder: Genesis is a complete desktop OS first; the local AI is one integrated capability, not the point of the desktop (done, decision 28)
- Founder's definition of Genesis, verbatim in spirit: an OS that can build anything with the AI: any app needed, connect with browsers, listen for voice, and more (accepted, decision 29)
- Browser control built: agentd gained browser_open / browser_read / browser_click / browser_type / browser_screenshot, driving a headless Chromium (Fedora `chromium` RPM) over the DevTools protocol with a throw-away profile, never the user's browser (built, decision 30)
- Voice input built: whisper.cpp v1.8.1 compiled in the image (`/usr/lib/genesis/whisper/bin/whisper-cli`; Fedora's package ships only the library), an optional `stt` model (ggml-base-q5_1, 57 MB) in every pack, `POST /api/transcribe` in agentd, hold-to-talk mic buttons in the maker, and genesis-window granting the microphone to local surfaces only (built, decision 31)

## 2026-09-10
- Base: Fedora bootc, forked from Universal Blue Aurora (KDE) (reconfirmed, decision 1)
- Inference: llama.cpp `llama-server` behind llama-swap, one OpenAI-compatible endpoint on localhost (accepted, decision 2)
- GPU: Vulkan on the host for all vendors; `genesis-nvidia` image flavour with open kernel modules; CUDA/ROCm/SYCL via ramalama containers (accepted, decision 3)
- Agent core: embed Goose (Rust, Apache 2.0, MCP-native) (accepted, decision 4)
- Default models: Qwen3.5 / 3.6 / 3.8 family (Apache 2.0) (accepted, decision 5)
- Licensing: three tiers (main / restricted / excluded) (accepted, decision 6)
- Sandbox tiers T0-T4: bubblewrap → containers → confirmation + snapshot → image rebuild → never (accepted, decision 7)
- Stack: Rust for daemons and UI; Python only in containers; TypeScript only for the browser extension (accepted, decision 8)
- v1 surfaces: launcher, agent workspace, model manager, terminal, select-to-act (accepted, decision 9)
- Phase 0 runs natively on the dev Mac first (Metal), Fedora VM second (accepted, decision 10)
