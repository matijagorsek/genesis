# Decision log

Short form. Evidence and alternatives are in [genesis-brief.html](genesis-brief.html).

| # | Date | Decision | Status |
|---|------|----------|--------|
| 1 | 2026-09-10 | Base: Fedora bootc, forked from Universal Blue Aurora (KDE). Re-examined against from-scratch and independent routes on 2026-09-11, see [base-decision.md](base-decision.md). Ubuntu 26.04 LTS is the runner-up. | reconfirmed |
| 2 | 2026-09-10 | Inference: llama.cpp `llama-server` behind llama-swap, one OpenAI-compatible endpoint on localhost. Ollama only as a compatibility shim. | accepted |
| 3 | 2026-09-10 | GPU: Vulkan on the host for all vendors; `genesis-nvidia` image flavour with open kernel modules; CUDA/ROCm/SYCL via ramalama containers. | accepted |
| 4 | 2026-09-10 | Agent core: embed Goose (Rust, Apache 2.0, MCP-native). OpenHands SDK optional in a builder container. | accepted |
| 5 | 2026-09-10 | Default models: Qwen3.5 / 3.6 / 3.8 family (Apache 2.0). Packs per hardware tier, downloaded at first boot as OCI artifacts. | accepted |
| 6 | 2026-09-10 | Licensing: three tiers (main / restricted / excluded). Default packs only from main. | accepted |
| 7 | 2026-09-10 | Sandbox tiers T0-T4: bubblewrap → containers → confirmation + snapshot → image rebuild → never. Agent runs as its own user. | accepted |
| 8 | 2026-09-10 | Stack: Rust for daemons and UI; Python only in containers; TypeScript only for the browser extension. | accepted |
| 9 | 2026-09-10 | v1 surfaces: launcher, agent workspace, model manager, terminal, select-to-act. Browser sidebar and voice in v2. No always-on screen memory. | accepted |
| 10 | 2026-09-10 | Phase 0 runs natively on the dev Mac first (Metal), Fedora VM second. Linux-only parts deferred to Phase 1. | accepted |
| 11 | 2026-09-11 | llama.cpp ships as the upstream Vulkan release build under `/usr/lib/genesis/llama.cpp`, not Fedora's `llama-cpp` package (months behind, no Vulkan backend, drags the full ROCm stack in, +2.5 GB). CUDA/ROCm builds via ramalama containers. | accepted |
| 12 | 2026-09-11 | First Genesis image built: `genesis:0.1` from `ghcr.io/ublue-os/aurora:stable`, 3.80 GB (base 3.70). `bootc container lint` is strict in CI, skipped in emulated local builds. | done |
| 13 | 2026-09-11 | First boot. Release v0.1.9: CI boot test green under KVM; the same image boots on the dev Mac under QEMU emulation in 90 s with `genesis-router.socket` listening. Pipeline: strict lint, self-check, push, qcow2, boot test, split release; ISO in its own job. | done |
| 14 | 2026-09-11 | Release v0.1.10: first fully green pipeline, both jobs. Installer ISO (5.2 GB, split) and qcow2 attached. Hostname still `aurora` (open). | done |
| 15 | 2026-09-11 | genesis-permd built: first Rust daemon. Intent envelope → tier (R0/R1/W1/W2/S1/S2/X/N) → verdict per session mode (assist / auto_edit / autonomous), taint raises W2+ to prompt, hash-chained audit log, TOML policy overlays, D-Bus `org.genesis.Permission1`, CLI `check`. 12 unit tests incl. the Phase 0 `--break-system-packages` case (S1, prompt). Ships in the image via a cross-compiling builder stage; CI diffs the shipped policy against the binary. | done |
| 16 | 2026-09-11 | Release v0.1.14: first image with genesis-permd inside; CI has a Rust test job and diffs the shipped policy against the binary; boot test exercises the broker in the VM. | done |
| 17 | 2026-09-11 | Hostname fixed in v0.1.17 via `systemd.hostname=genesis` in `/usr/lib/bootc/kargs.d`. Root cause: `/etc/hostname` written in the Containerfile never reaches the deployment (build-time bind mount; deployment /etc handling), and Aurora's prebuilt initramfs names the machine from its own os-release. | done |
| 18 | 2026-09-11 | genesis-probe built: detects GPUs (sysfs, nvidia-smi), RAM, CPU flags, free disk; computes a memory budget (VRAM, unified 75%, or 60% RAM); evaluates the shipped packs' triggers with per-model `optional` and `offload` flags; ranks by platform specificity then trigger tier; writes `/etc/genesis/profile.json` from a first-boot oneshot. 6 tests; packs ship in the image under /usr/share/genesis/packs. | done |
| 19 | 2026-09-11 | Release v0.1.21: first boot writes `/etc/genesis/profile.json` via genesis-probe.service; the CI VM (4 GB, no GPU) gets pack `tiny`. bootc-image-builder rejects `customizations.hostname` for qcow2 (hence the earlier dead end); hostname stays on kargs.d. | done |
| 20 | 2026-09-11 | genesis-firstrun built: first-run wizard as a local web UI + JSON API on 127.0.0.1:11510 (root service until `/var/lib/genesis/first-run-done`; user unit opens it at first graphical login). Reads the probe profile, proposes packs with reasons, plans downloads from the Hugging Face API, downloads with resumable curl and live progress, renders `/etc/genesis/router.yaml` from the installed files, records the privacy choice, writes the done marker and exits. Verified end to end locally on a 640 MB test pack. Account creation stays with the installer for now. | done |
| 21 | 2026-09-11 | Release v0.1.22: first-run wizard service active at first boot in the VM; its API answers from inside the booted system (recommended pack `tiny` on the 4 GB CI VM). | done |
| 22 | 2026-09-11 | genesis-agentd built: tool-calling loop on the local endpoint (shell, read/write/edit file, list dir), every call converted to a permd intent and gated in-process (allow / prompt-and-wait for a client / deny), bubblewrap shell sandbox on Linux, local API on 127.0.0.1:11520 for launchers plus headless `run`. permd became a library. 6 tests with a canned model. First real run on the Mac with the local 27B: 6 turns, task complete, all calls gated. | done |
| 23 | 2026-09-11 | genesis-txd built: transactions with two snapshot backends (btrfs subvolume snapshot when on btrfs as root; exact file pre-images otherwise), a ledger (files, btrfs, flatpak user refs, shell commands marked not undoable), commit and rollback, CLI `list / show / undo`. agentd opens a transaction on the first allowed W2/S1/S2 action, saves pre-images before each write, commits at the end, exposes `POST /api/sessions/{id}/undo`. | done |
| 24 | 2026-09-11 | Gap found by a real run and closed: shell redirects and mutating commands naming absolute or `~` paths outside the project are now W2 (system paths S2), with those paths pre-snapshotted by agentd. Verified: out-of-project edit undone from the CLI. | done |
| 25 | 2026-09-11 | Course correction from the founder: the product is "tell the OS what you want made and it makes it", not a chat. The workspace became a maker surface (What do you want to make? → templates → scaffold → live preview in the middle → steer → install as app). agentd gained maker tools: `list_templates`, `scaffold`, `preview_start/stop`, `install_app`; templates ship at /usr/share/genesis/templates. Verified: a pomodoro web app built by the local 27B from one sentence, running in the preview. | done |
| 26 | 2026-09-11 | Launcher on Plasma: `genesis-krunner`, a KRunner D-Bus runner (`ask …` → "Ask Genesis" → opens the workspace with the prompt), a desktop entry, and a Meta+Space default in /etc/xdg/kglobalshortcutsrc. Linux-only; compiled on Linux in CI. | built |
| 27 | 2026-09-11 | Found on the v0.1.26 boot capture: the "Welcome to Plasma Desktop / Begin Setup" screen is Plasma Setup (KDE's out-of-box account wizard, run by `plasma-setup.service` until `/etc/plasma-setup-done` exists), not the removed Aurora welcome app; the login manager's wallpaper is `/usr/lib/plasmalogin/defaults.conf`. Genesis masks Plasma Setup (installer + Genesis wizard own accounts) and sets the login wallpaper to Genesis. | fixed, verifying |
| 28 | 2026-09-11 | Clarified with the founder: Genesis is a complete desktop OS first; the local AI is one integrated capability, not the point of the desktop. The image now ships a full default app set as RPMs (Firefox, Okular, Gwenview, KCalc, Discover with Flatpak backend, Haruna, Elisa, KDE Connect, KWalletManager, Partition Manager) on top of Aurora's Dolphin, Konsole, Kate, Ark, Spectacle, System Monitor, Filelight, and a Flatpak preinstall list (LibreOffice, Thunderbird, VLC, GIMP, Krita, Bazaar) for once the machine is online. | done |
| 29 | 2026-09-11 | Founder's definition of Genesis, verbatim in spirit: an OS that can build anything with the AI: any app needed, connect with browsers, listen for voice, and more. The complete desktop (decision 28) is the base; the agent capabilities are the point. Browser control (Playwright MCP, isolated profile) and voice (Whisper in, Kokoro out), both planned for v2 in decision 9, move up to the next milestones after the real desktop screenshots. | accepted |
| 30 | 2026-09-11 | Browser control built: agentd gained browser_open / browser_read / browser_click / browser_type / browser_screenshot, driving a headless Chromium (Fedora `chromium` RPM) over the DevTools protocol with a throw-away profile, never the user's browser. Pages come back as text plus a numbered element list. Gating: navigate is R1 (network, prompts in Assist), click/type W1, read R0; every page read taints the session so anything at W2 and above asks a human again. Verified against a local page with real Chromium in the test suite. | built |
| 31 | 2026-09-11 | Voice input built: whisper.cpp v1.8.1 compiled in the image (`/usr/lib/genesis/whisper/bin/whisper-cli`; Fedora's package ships only the library), an optional `stt` model (ggml-base-q5_1, 57 MB) in every pack, `POST /api/transcribe` in agentd, hold-to-talk mic buttons in the maker, and genesis-window granting the microphone to local surfaces only. Audio is recorded as 16 kHz WAV in the page, so no ffmpeg is needed. Everything stays on the machine. | built |
| 32 | 2026-09-12 | Voice output built: Piper (upstream static Linux build, 26 MB, with its espeak-ng data and onnxruntime) under `/usr/lib/genesis/piper`, an optional `tts` voice (en_US-lessac-medium, 70 MB) in every pack, `POST /api/speak` in agentd returning WAV, and a speaker toggle in the maker that reads replies aloud. Verified: the shipped Linux build speaks inside a Fedora 44 container; the macOS build lacks its espeak-ng library, so the Mac unit test skips. | built |
| 33 | 2026-09-12 | Design plan after three research passes (Plasma identity, what wows in 2025–26 products, agent interaction design): identity comes from colour + wallpaper + type + dock + boot-to-login continuity, not replaced widgets; twelve wow moments ranked; ten behaviour rules (one door at Meta+Space, plan card, plain-verb prompts, Keep/Undo, provable "on this machine", voice acknowledges in 300 ms, first run ends with something made). Full plan: docs/genesis-design-plan.html. Phase A (identity) implemented: GenesisDark colour scheme, procedural wallpaper, Inter + IBM Plex Mono, Papirus-Dark, Ocean sounds, centered floating dock with pinned apps, KRunner free-floating, blur on, no window borders, Splash.qml, Plymouth watermark, defaults under /usr/share/kde-settings. | accepted |
| 34 | 2026-09-12 | UX pass on the two surfaces people touch, against the ten rules: starter prompts under the maker's field; modes renamed Ask / Trusted / Hands-off with plain descriptions; permission cards say "Genesis wants to install software / use the network / change a file outside the project", with Not now / Allow once (tier codes only in tooltips); allowed decisions no longer clutter the step list; a Keep / Undo toast after each job that touched anything outside the project; the wizard's last step offers three things to make, and genesis-window opens the maker with that prompt the moment the desktop starts. | built |
| 35 | 2026-09-12 | First real desktop session through the dev loop (v0.1.34 in QEMU, SSH, plasmashell scripting over D-Bus) exposed that every image so far shipped no panel: the look-and-feel layout script used `new Desktop()`, which Plasma 6 layout scripting does not have, so it died on line 3. Fixed with `desktopsForActivity()`; the dock, launcher, pinned apps and app menu were verified live. Also verified live: the maker window opening after the wizard, and the new Genesis status widget (`org.genesis.status`: model, network use, sandbox, "Open Genesis"), now in the dock next to the clock. sshd on test disks is gated by the kernel argument `genesis.devssh=1`, because bootc-image-builder ignored the services customization. | done |
| 36 | 2026-09-12 | Select-to-act built and verified live in the VM: Meta+Shift+Space runs `genesis-ask-selection`, which takes the Wayland selection and opens the maker with "About this selection: …" as the request. Text goes only to the local agent. | built |
| 37 | 2026-09-12 | Genesis Settings, first version: a page in the Genesis window (`/settings`, menu entry under Settings) built from local sources only: hardware and model service state, models on disk, default autonomy mode (stored in ~/.config/genesis/settings.json and picked up by the maker), voice readiness, undo history from the transaction ledger. A native System Settings module can replace the shell later; the data endpoints stay. | built |
| 38 | 2026-09-12 | One door: `Meta+Space` now opens the palette (`genesis-window --palette`, page `/palette` from agentd): one field, hold-to-talk, a chip for the current selection, starter prompts, Enter hands the request to the maker and the window grows into it, Esc closes. Select-to-act routes through the same window with `--selection`. The dock icon still opens the full maker. | built |
| 39 | 2026-09-12 | Polish for the VM path: voice acknowledges within milliseconds of the press (chime + glow in the palette and the maker, before the microphone even opens; a lower tone on release), and Settings shows the measured speed of the last reply (tokens per second, recorded around every completion). Gallery export bundles and wizard fit bars landed the same day. | built |
| 40 | 2026-09-12 | One-key theme swap: Genesis Light colour scheme (same accent, light neutrals), `genesis-theme dark|light|toggle` (colour scheme + Papirus variant for every app, panel and login), bound to Meta+Shift+T and offered as Day / Night buttons in Genesis Settings. | built |
| 41 | 2026-09-12 | Found in a real v0.1.47 session: the user-level permission broker service never started in any image (ProtectSystem=strict with ReadWritePaths to a directory that did not exist yet: exit 226/NAMESPACE). The maker was unaffected because agentd gates in-process, but the D-Bus broker for other apps was down. Fixed with StateDirectory=genesis. | done |
| 42 | 2026-09-12 | Terminal: `genesis-ask` (alias `ask`) turns a sentence into one command via the small local model, classifies it with the shared permission policy ("reads only", "changes files here", "changes the system", refused), and runs it only on confirmation; `--print` feeds a bash Ctrl+G binding that rewrites the typed line into the command. Verified natively against the local model service: 4 s cold, sub-second warm. | built |
| 43 | 2026-09-12 | Found with a real model download in the VM: the router service crash-looped since the first image because a socket unit held 127.0.0.1:8080 while llama-swap binds the port itself (no socket-activation support). The boot test only saw the socket unit answer. Socket activation removed; genesis-router.service is a plain enabled service with ConditionPathExists on the rendered router config, and the wizard starts it after rendering. | done |
| 44 | 2026-09-12 | First real model run inside the VM on this Mac: the wizard downloaded the tiny pack (1.9 GB) over QEMU user networking, the router served it, and a chat completion returned a real reply (18 tokens; 10 minutes wall time on the emulated x86_64 CPU, almost all of it model loading). agentd now picks the best model the router actually serves when its configured model is absent (the tiny pack has no "code"). | done |
| 45 | 2026-09-12 | Limit found: on the emulated x86_64 VM on Apple Silicon the model service works but is far too slow to use (a 21-token prompt took 10 minutes including load; the `ask` prompt timed out at 10 minutes). Not a product bug: the same path is fast on real hardware. Aurora publishes amd64 only; Fedora bootc is multi-arch. An arm64 Genesis flavour on `quay.io/fedora/fedora-bootc` with Plasma installed by us would run natively on this Mac under Apple's hypervisor at full speed. Proposed to the founder as the next step for the VM path. | proposed |
| 46 | 2026-09-13 | arm64 flavour built and verified natively on an Apple Silicon Mac: plain Fedora bootc 44 base with Plasma installed by the image, all Genesis pieces, strict lint and a clean self-check. Under Apple's hypervisor (`prototype/vm-dev-arm.sh`): boot to login in under a minute, the tiny pack downloaded through the wizard, the model service up. Measured on the 2B model, CPU only: first reply 27 s including load, warm generation about 6 tokens/s, a warm `ask` 25 s (long prompt). About 25x the emulated x86_64 VM. Next speed step: a native arm64 llama.cpp build with CPU features enabled (the generic upstream tarball is used now). CI: `image-arm64` job on ubuntu-24.04-arm attaches `-arm64` disk parts to every release. | done |
| 47 | 2026-09-13 | Tuned inference for arm64 and VMs. llama.cpp for arm64 is now built from source with every CPU variant (chosen at run time) and Vulkan. Measured in the native VM: the build was not the bottleneck; using all 6 vCPUs for generation thrashed (1.6 tok/s) while 3-4 threads gave 7 tok/s, and a software Vulkan device (llvmpipe) was offloaded to for nothing. The wizard now renders `-t cores-2` (min 2) and `-ngl 0` when the only GPU is virtual or software, `-ngl 99` otherwise. Result in the VM: a 53-token reply in 8.8 s, `ask` in 7 s (from 20-25 s). | done |
| 48 | 2026-09-13 | First maker run inside Genesis itself, on the tiny pack's 2B model in the native VM: 12 turns; it planned, scaffolded, read files, ran the test command, asked permission for a package install exactly as designed, then declared success without implementing the request. Honest result: the tiny pack cannot build apps (its description says so). Product responses: the maker shows a notice when only the small model is served; the agent nudges once when the model tries to finish right after scaffolding without changing a file; pytest ships in the image; scaffolds always go into a subfolder. A real test of the maker's promise needs a "code" model, i.e. a GPU-class pack on real hardware or a 16 GB Mac VM with the gpu-16 pack. | done |
| 49 | 2026-09-13 | Found by installing the cpu pack in the VM: the test disk is 11 GB, the pack 10 GB; the download ran out of space mid-file, and the API let me finish anyway, leaving a system with no router config. Fixes: the wizard API refuses a pack the profile says does not fit (unless forced) and refuses to finish during or after a failed download (unless forced); test disks stay at the builder default (a disk customization crashed bootc-image-builder); a resized qcow2 grows its root at boot. | done |
| 50 | 2026-09-13 | Measured the "cpu" pack's 9B code model inside the 16 GB native VM: prompt 6 tok/s, generation 0.9 tok/s on 6-8 vCPUs, so a maker turn with a few thousand tokens of context takes minutes and the job timed out. CPU-only 9B is not usable for the maker in a VM on this Mac (it is fine on a real 16 GB desktop with a GPU, and the fast/tiny models are fine on CPU). For development and demos on the Mac, a dev bridge (`just vm-bridge`) points the VM's agent at the Mac's own Metal-accelerated model service through QEMU's host address; the installed OS never uses another machine. | done |
| 51 | 2026-09-13 | The promise, end to end, inside Genesis: in the native arm64 VM, the maker took "a checklist app that saves to a file", scaffolded into its own folder, wrote a real CLI with add/done/undone/remove/clear, atomic JSON saves and seven unit tests, found and fixed three of its own bugs (a typo, a shadowed test method, an id-counter bug), ran a demo, and cleaned up: 17 turns, all tests passing. Model: the 27B code model on the Mac's GPU through the dev bridge; the VM's own CPU is enough for the desktop, voice and `ask`, not for app building. Transcript and result in examples/checklist-in-vm/. | done |
| 52 | 2026-09-14 | Update channel: an installed Genesis follows the image it was installed from (`ghcr.io/matijagorsek/genesis:0.1` or `:0.1-arm64`). bootc's update timer is enabled, but its service is overridden to stage only (`bootc upgrade`, no `--apply`): updates download in the background and apply at the next restart, never rebooting on their own. A small root service publishes `bootc status` to /run/genesis for the desktop; Genesis Settings shows the running image, a staged update, and a "Check for updates now" button that starts the update service through a polkit rule for administrators. | built |
| 53 | 2026-09-14 | Distribution basics: a download page at https://matijagorsek.github.io/genesis/ (GitHub Pages from docs/) that shows the latest release and the right file per platform; every pushed image is signed with sigstore (cosign, keyless with the workflow's GitHub identity), verifiable with `cosign verify --certificate-identity-regexp github.com/matijagorsek/genesis --certificate-oidc-issuer https://token.actions.githubusercontent.com ghcr.io/matijagorsek/genesis:<tag>`. Signature enforcement on the client (containers policy) comes once the identity is stable, so a policy mistake cannot strand installed systems. | built |
| 54 | 2026-09-14 | NVIDIA flavour: `ghcr.io/matijagorsek/genesis:0.1-nvidia`, the same Genesis layer on Aurora's nvidia-open base (open kernel modules), built, self-checked, pushed and signed on every release. No disk image or boot test in CI: NVIDIA needs real hardware. An installed x86_64 Genesis switches with `bootc switch ghcr.io/matijagorsek/genesis:0.1-nvidia` and reboots. | built |
| 55 | 2026-09-14 | Genesis in System Settings: `kcm_genesis`, a QML module (KCMUtils 6) under System Administration, showing updates, the machine, models on disk, the default autonomy, day/night, voice and undo, all read and written through the local Genesis daemon. Built in the Qt stage next to genesis-window. The Genesis Settings page stays as the same data in the Genesis window. | built |
| 56 | 2026-09-14 | Small pieces of the "later" list: a python-web template (standard-library server with a JSON API and a front end, verified); KDE Connect allowed through the firewall on the plain-Fedora flavour and mentioned at the end of first run; the first proactive card, a staged update, shown in the dock widget with a green dot and a "restart to apply" line. Remaining from the plan: context cards for repos and files, and Node/Rust templates via distrobox. | built |
| 57 | 2026-09-14 | Proactive cards, first two kinds: a made project whose last run ended with an error ("Fix it" hands the log to the maker) and a made project never installed to the menu ("Install"). Served by `/api/notices` from local files only, shown on the maker's start page above the starters. Together with the staged-update card in the dock widget this closes the "proactive" item of the plan at a first level; repo and calendar cards can follow the same shape. | built |
| 58 | 2026-09-14 | Claude, the login way and no API keys: the founder asked for account sign-in rather than keys. Third-party apps cannot use a Claude account's login, so Genesis includes Claude Code, Anthropic's own terminal agent, as an optional app: a "Claude" menu entry and an "Open Claude" button in Settings and in System Settings, plus "Open in Claude" on every made project. First start installs it into the home folder with Anthropic's installer and asks for the account sign-in in the terminal. Genesis' own maker, palette and `ask` stay local; nothing goes to Claude unless the person opens a project there. An earlier API-key provider was built and then reverted the same day. | built |
| 59 | 2026-09-14 | Toolchains without touching the OS: `genesis-toolbox node|rust -- cmd` creates a distrobox per language on first use (Fedora + nodejs/npm or cargo/rustc) with the home folder shared; two new templates use it, node-web (Node HTTP server with a JSON API and a front end) and rust-cli (argument parsing and a test). Verified in the arm64 VM: first use creates the Node box in 6 minutes (Node 22) and the Rust box in 4 minutes (cargo 1.98), then commands run directly. | built |
| 60 | 2026-09-14 | Signature enforcement: images are now signed twice, keyless (GitHub identity, for people) and with a Genesis release key (for machines). The private key and its password live only as repository secrets; the public key ships in the image at /etc/pki/genesis/cosign.pub, and /etc/containers/policy.json requires a valid signature by it for ghcr.io/matijagorsek/genesis, so `bootc upgrade` and `bootc switch` refuse an unsigned or tampered image. Everything else keeps Fedora's default policy. Also from this batch: toolboxes for Node and Rust, richer proactive cards, honest per-pack expectations at first run, accessibility for the palette and the maker, the permission audit visible in Settings, generated release notes, and a native arm64 capture script. | built |
| 61 | 2026-09-14 | Small models and the maker: a compact mode for the "fast" class (2B-4B): a short strict script (scaffold, read, write the entry file, run, fix, summarise) and nine tools instead of nineteen. Measured with the 4B model on the Mac: it chose the right template (python-web) and finished in 7 turns with a working app, but its "rewrite" of the entry file was byte-identical to the template. The agent now reports such writes as unchanged and asks for the real change. With that in place and a request the template cannot satisfy ("with a due date on each item"), the same 4B model changed both files, added due dates to the API and the page, and the app worked: 8 turns, about a minute. Small models can extend a template; the wizard still states the honest ceiling per pack. | built |
| 62 | 2026-09-14 | The update channel, verified against the real registry after the GHCR package went public: the native VM pulled `ghcr.io/matijagorsek/genesis:0.1-arm64` with `bootc switch` (3 GB, 12 minutes), staged it, and after a reboot runs that CI-built image with the login screen, the model service and the status publisher up. Anonymous pulls work; the keyless signature verifies from outside with the workflow identity, and the release-key signature on v0.1.92 verifies with the shipped public key. A following `bootc upgrade` pulled the first release-key-signed build (2.5 GB, 13 minutes); after a reboot the policy and public key are in place and `bootc upgrade --check` succeeds under that policy, so enforcement is live and does not lock installed systems out. | done |
| 63 | 2026-09-14 | Policy review with an adversarial test battery (24 commands a model might emit). Three real gaps found and closed: reading a key through a shell command (`cat ~/.ssh/id_ed25519`) was plain read-only, now any secret path named in a command is a secrets action; changes to login files (`~/.bashrc`, autostart, user units, crontab) were merely "outside the project" and could run unasked in Hands-off mode, now they are user-scope system changes; piping the network into a shell (`curl … | sh`) was allowed in Hands-off, now it is a system change that always asks. Genesis' own settings and Claude Code's state are secrets. The battery stays in the test suite. | done |
| 64 | 2026-09-14 | Hygiene and reach for a public project: LICENSE (Apache-2.0) and NOTICE with every redistributed component's licence, CONTRIBUTING, SECURITY, a code of conduct and issue templates including a hardware report. Localisation: the surfaces people see first (palette, maker start page, wizard) follow the system language, with German and Slovenian dictionaries; a language is a dictionary. Voice: the more accurate "small" speech model on machines with 16 GB or a GPU, the most capable model on disk is used, replies are English only for now and Settings says so. A phone-pairing card and hardware notes on the download page. CI now captures screenshots of every x86_64 release under KVM, attaches them to the release and publishes them to docs/screens/latest. | built |
| 65 | 2026-09-14 | Phone, day one. KDE Connect has no QR pairing in any version, so first run gets a "Pair your phone" step that lists visible phones and shows the 8-character code both sides display; the same list lives in Settings. `genesis-phone` (Python over D-Bus) does status, pairing, custom addresses, and registers four commands on every paired phone (ask what I shared, day / night, lock, apply the staged update). Text shared from the phone lands in the clipboard; a user service watches it with `wl-paste --watch` and answers anything starting with "ask" or "genesis" through the local model, replying with a KDE Connect ping so it shows as a phone notification; "make …" starts the maker on the desktop instead. Same network only, deliberately no relay. Verified in the arm64 VM against an Android emulator: pair, commands, question, answer. | built |
| 66 | 2026-09-15 | Packs as signed OCI artifacts. The artifact is the pack definition resolved to exact files, sizes and sha256 (from the Hugging Face API), pushed by the publish-packs workflow to ghcr.io/<owner>/genesis-packs:<pack> and signed keyless and with the release key. Machines pull it with skopeo under the same containers policy as the OS (podman artifact pull cannot store signatures), keep it per user, and the wizard verifies every download against it; without network the built-in list still works. Model blobs stay on Hugging Face for now: moving them is a URL change, not a format change. | built |
| 67 | 2026-09-15 | Local file index. `genesis-index`: SQLite FTS5 over folders the user opts in (Settings > Files Genesis may search), text from plain files, code, PDF (pdftotext) and office XML, refreshed by a user timer every 20 minutes. The maker gets a `search_files` tool classified read-only; it says when nothing matches instead of guessing. Embeddings later, once measured to help. | built |
| 68 | 2026-09-15 | Ask about the screen. Meta+Shift+A (Meta+Shift+Space is the selection), Spectacle region, question, local vision model via the router (`fast` with a projector). Every pack now includes the projector (tiny +0.7 GB). Verified in the arm64 VM on CPU: a correct description of a 1000x620 region in 7 s with the 4B model. | built |
| 69 | 2026-09-15 | Phone, further. Voice notes sent to Genesis are transcribed with whisper and answered (verified: "ask what is the tallest mountain in Europe" came back as Elbrus on the phone). The palette lists the phone's notifications and replies through KDE Connect where the app allows it. In the emulator the notification sync bound but delivered nothing yet; the code path is in place and still has to be verified on a real phone. | partly |
| 70 | 2026-09-15 | Security review of the Rust daemons (independent adversarial read of the code, deployment and supply chain). Two findings the permission model could not see: the local HTTP APIs accepted requests from any web page (drive-by control of the maker, DNS rebinding for reads), and previews ran the project's dev command outside the sandbox at the lowest tier although the model can rewrite it. Fixed: both daemons now refuse anything that is not their own origin (Host, Origin, Sec-Fetch-Site) and require a per-boot token on every state-changing call; pages get the token when served, local helpers read it from the runtime directory; request bodies are capped; /api/open only opens what Genesis made or exported and never launchers; a preview command that is not the shipped template's own is classified as the command it is and as a change to the project; voice notes are taken only from files KDE Connect delivered; builds use the lockfile and CI runs a RustSec audit. Verified in the VM: 401 without token, 403 for foreign origin, cross-site and rebound Host, 413 over the limit. Left for later, in order: enforce the network denylist for sandboxed shells that ask for network, canonicalise paths before secrets classification, pin the base image by digest and checksum upstream binaries, key the audit chain. | built |
| 71 | 2026-09-15 | Fresh-disk run of a downloaded release, as a new user: 0.1.102 arm64 from GitHub, checksums verified, first login, wizard recommends the tiny pack on 8 GB and downloads it, the starter make builds the checklist app, a second make from the launcher builds the pomodoro script with the 2B model on CPU in about four minutes. Two things found and fixed on the way: the wizard's hardware page did not follow a download started elsewhere, and a launcher entry named "Ask about the screen" outranked "Ask Genesis" in menu search. Genesis moves to the 0.2 series: versions 0.2.N, a floating `stable` tag that installed machines follow (0.1 is still pushed for machines installed before), release notes written for people in docs/releases/0.2.md. Model files for the tiny and cpu packs are mirrored on GHCR as checksum-addressed artifacts and the downloader prefers them. | built |
| 72 | 2026-09-15 | Security follow-ups from the review, all four: the secrets and system rules match on the canonical path (every spelling of home, symlinks resolved, so a link in the project pointing at a key is a key); a sandboxed shell that asks for network and names a denied host (cloud metadata, loopback services) is refused outright, since a domain filter cannot reach inside a shell; the audit chain is keyed with a per-install secret (HMAC-SHA256, 0600 next to the log), so editing the log without the key breaks the chain; the three base images are pinned by digest with a weekly job that moves the pins forward as a reviewed commit, and the Piper and llama-swap tarballs are verified by checksum before extraction. The RustSec step found one advisory (rustls) on its first run; fixed by updating. Battery cases added for the new rules. | built |
| 73 | 2026-09-15 | First real upgrade across the series: the fresh VM installed from 0.1.102 switched to the `stable` channel, pulled 0.2.107 under the signature policy, rebooted into it with projects, models and settings intact, and the new user units (packs, index, phone) came up enabled. One self-check fails on that machine afterwards: with the models already on the 5 GB volume, no pack "fits" any more, so the probe names no recommended pack. That is a probe rule to fix (a pack whose files are already present fits by definition), not an upgrade problem. | built |
| 74 | 2026-09-15 | First real phone: an iPhone paired with Genesis running in the Mac VM, added by address from the Genesis side since the VM sits behind QEMU's NAT and cannot be discovered; the phone answers back through the forwarded port. Pairing, commands and sharing are the iOS scope; notification sync is Android-only by Apple's rules. | verified |
| 75 | 2026-09-15 | Improvement review (independent read of every surface, the daemons, units, workflows and docs; twenty findings). Fixed in one pass: the Settings page broken by duplicate ids from the new section navigation; model setup could not be reopened after "later" (a "Set up models" entry in the app menu now removes the marker and restarts the wizard); the signed pack definitions were fetched per user while the wizard runs as root, so nothing was ever verified at first run (a system service fetches them before the wizard, and the wizard reads that copy); the workspace microphone release recursed; "make" from the phone sent an empty project; two shortcuts shared Meta+Shift+Space (screen question is Meta+Shift+A); LAN-supplied device names were injected into pages; CPU-only machines now get the compact tool set, a shorter context and reasoning budget; a toolbox that has to be created counts as network use; a pack larger than the free space is refused with a plain sentence; boot-test checks now fail the job; the nightly rebuild is gone (the weekly base bump covers it); the download page no longer guesses arm64 for every Mac. Docs corrected where they overclaimed (plan rules marked partial, localisation "partly", the mode wording). Second pass, same day: a "no models yet" banner with the start button disabled when the router is down, an elapsed-time line after ten seconds on a running step, undo from Settings through the transaction store, polling back-off when idle, keyboard hold-to-talk, progress-bar semantics and higher contrast, checksum links on the download page, contributor prerequisites, stale Goose tasks removed. Verified in the VM: the Settings page renders in full again, the root wizard sees all eight signed pack definitions, the setup entry is in the menu. | built |
| 76 | 2026-09-15 | Phone app, started. Not a replacement for KDE Connect (pairing, transfers, notifications stay there) but a Genesis companion: `genesis-companiond` serves a TLS API on the local network (self-signed certificate whose fingerprint the phone pins, a long pairing secret as bearer token, both in a QR under Settings > Phone app; regenerate to unpair every phone). It proxies to the maker with the daemon's own token, so the broker and its cards apply unchanged: jobs, steps, permission answers, a make from the phone, one screenshot on demand, never a stream. Same network only. The Android app (Kotlin, Compose) is built and verified on a real phone the same day: the founder's Samsung, on USB, paired through the Mac to Genesis in the VM, asked for a word-count tool and watched it get made with the permission decisions listed; a screenshot of the computer on demand. iOS later if wanted. A user guide, docs/how-to-use.md, joins the contributor documents. | built |
| 77 | 2026-09-15 | Phone app, second pass, verified on the founder's phone: a quiet foreground service watches the paired Genesis and raises a notification with Allow once / Not now when a job waits for an answer (the answer goes through the same permission API), and one when a job finishes; a "Made" tab lists what Genesis built; the companion daemon honours the default mode from Settings. Release builds are signed with a keystore kept outside the repository (~/.config/genesis-android), so updates install over the previous version. Verified end to end: a job started in assist mode paused on its first step, the phone showed "Genesis wants to scaffold", Allow once from the notification shade let it continue. | built |
| 78 | 2026-09-15 | Babel, the Genesis IDE. Not an editor of our own: Code-OSS through VSCodium (MIT, telemetry off, pinned by version and checksum), unpacked to /usr/lib/babel and branded, so every language VS Code speaks works on day one, with debuggers, terminals and the extension ecosystem. What makes it Genesis is a built-in extension in plain JavaScript (no build step): the maker as a sidebar view on the open workspace, permission cards in the sidebar and as editor notifications with the desktop's plain verbs, "Change this file…" and "Make something…" from the context menus and Ctrl+Alt+Space, "Ask about the selection" into an output channel, Undo for the job, recent jobs, and the Genesis Dark theme; it talks to the maker through the same token front door as every other surface. Named by the founder from five options; "Babel" for the tower where every language meets. Verified in the VM the same day: Ctrl+Alt+Space, "add a seconds option to pomodoro.py", the sidebar showed the edit, the preview and the reply, with Undo one click away. Two things found on the way: Electron wants to be told it is on Wayland, and the maker answers 201 when a job opens. Babel then got its own face so it does not read as VS Code: the Babel mark in the title bar, the dock and behind empty editors, the tool rail on top of the sidebar, no minimap, no chat panel, the title ends in Babel, a small stylesheet for the accent, and the core-file checksums kept true after branding so Code-OSS does not call itself corrupt. Polish the same day: the rail wears the Babel mark, "Made here" in the sidebar opens any made project in Babel, "Open in Babel" in the maker's gallery, and a welcome page with the Genesis rail focused on first start. | built |
| 79 | 2026-09-15 | What was missing, addressed: a weekly canary (a release from a week back boots under KVM, switches to the stable channel, reboots, and must come up on the new version with the wizard answering and the self-check run; serial logs attached); backup and moving (`genesis-backup`: projects with recipes, Genesis settings, search folders, phone pairings, undo history; never models, keys or passwords; Settings has Export and Restore); day two in the maker (a rotating next idea once something was made, and "Make this from a recipe" that replays a shared recipe with the same cards); German and Slovenian for the Settings page and accessibility labels on the gallery; ROADMAP.md, CHANGELOG.md derived from the decision log, labels and first issues for contributors, and docs/examples.md with the real runs as recipes. | built |
| 80 | 2026-09-15 | Babel understands the common languages out of the box. The editor-side extensions (Python with its debugger, Rust, Go, clangd) come from Open VSX pinned by version and checksum and are unpacked into Babel as built-ins; the servers come from the image (rust-analyzer and clangd from Fedora, gopls built from its tag in a build stage, TypeScript from Code-OSS itself, Python through the extension's bundled Jedi). The launcher points the extensions at the installed servers and turns their self-updates off. Compilers and toolchains stay in the toolboxes, as before; the OS carries servers, not build systems. Verified in the VM: the extensions load and Python files get language features; the servers themselves arrive with the next image. | built |
| 81 | 2026-09-15 | Debuggers in Babel without downloads: lldb-dap from Fedora's lldb package for Rust and C/C++ (the LLVM lldb-dap extension, pinned from Open VSX; rust-analyzer told to use it), Delve from Fedora for Go, debugpy already with the Python extension. The launcher points every extension at the installed binary and turns their self-updates off. | built |
| 82 | 2026-09-16 | Base images pinned on our own mirror. Pinning to upstream digests was not safe: quay.io garbage-collected the fedora-bootc digest we had pinned within a day and every arm64 build failed. The weekly bump job now copies the four bases (Aurora, Aurora NVIDIA, Fedora bootc, the image builder) into ghcr.io/<owner>/genesis-base and pins to the mirror, which never loses a digest; pins live in .github/base-pins.env and the Containerfile, because a workflow token may not edit workflow files (the first bump run failed on exactly that). Jobs log in to GHCR before the first pull. | built |
| 83 | 2026-09-16 | Recipes as a first-class thing in the maker: a Recipes row on the start page with six that ship with Genesis (the real runs, as steps) and the user's own, one click to replay through the same permission cards; "Save recipe" in the gallery keeps a made project's steps; right-click deletes yours; pasted recipes still work. Recipes are JSON files (/usr/share/genesis/recipes, ~/.local/share/genesis/recipes) so sharing one is sending a file or a few lines of text. | built |
| 84 | 2026-09-16 | The "missing" list closed out, and the base mirror: the weekly canary is now a gate (the build asks for the last completed canary run; if it was red the versioned tags still publish but `stable`, `stable-arm64` and `stable-nvidia` stay put until someone looks, or re-runs the build with `ignore_canary=true`); German and Slovenian are complete for the maker, the palette and Settings (every static string, checked by a script that lists what has no translation); a screen-reader pass with the real thing: Orca in the arm64 VM, the Genesis window started with Qt accessibility on, the AT-SPI tree walked: 94 controls, all named, the web content exposed once an assistive tool connects (Qt WebEngine exposes it lazily, which is normal); the two gallery buttons that read as bare "Continue" and "Open in Claude" now say which project. The dev launcher notes the one QEMU pitfall seen today: the guest follows the window size, and a window that is still tiny when the firmware starts can hit a console assert and stay black (relaunch). The `genesis-base` mirror package is private by default on GHCR, like `genesis` was; local builds on another machine need `podman login ghcr.io` until it is made public in the package settings, which only the owner can do; done the same day, all four mirrored bases answer anonymous pulls. | done |
| 85 | 2026-09-16 | Found by the canary once it printed disk use: on the x86 flavours a resized disk was never used. bootc's fallback grow unit is conditioned on `/usr/bin/growpart`, which the Aurora base does not carry (the Fedora bootc base for arm64 does, which is why the arm64 VM grew and the x86 canary sat at 96% of a 22 GB root inside a 38 GB disk). `cloud-utils-growpart` is now in the image, the self-check asserts it, and the canary prints `df` after every upgrade. The README's "the root grows on boot" was true only on arm64 until this build. | done |
| 86 | 2026-09-16 | Local completion in Babel, the first of the "missing from the brief" list: an inline completion provider in the Genesis extension calls llama.cpp's fill-in-the-middle endpoint through the router, with the pack's `fim` model when there is one and the always-loaded small model otherwise (Qwen3.5-2B answers `/infill` correctly; measured 10 s cold and under 2 s warm on the CPU-only arm64 VM). 250 ms debounce, 2400 characters before and 800 after the cursor, 48 tokens, cancelled requests are not failures (the first version treated a cancel as a dead router and went silent for 30 s), a status-bar item names the model and toggles it, a `Genesis completion` output channel logs every request. Verified in the VM with a screenshot of the grey suggestion. Found on the way: the debugpy extension failed to activate in every Babel so far because it writes an endpoint file next to itself under read-only `/usr`; its folder is now a symlink to a tmpfiles-managed `/tmp/.babel-debugpy`, and the self-check asserts both. The router config renders a `fim` model in the hot group when a pack ships one. Also: on Aurora `growpart` lands in `/usr/sbin`, which is not merged into `/usr/bin` there, so the image links it where bootc's unit looks. | done |
| 87 | 2026-09-16 | MCP servers as the extension point, and the maker as system administrator on top: `genesis-agentd` has an MCP client over stdio (JSON-RPC lines; initialize, tools/list, tools/call; reader thread with timeouts; a server that dies restarts on the next call). Servers are declared in `/usr/share/genesis/mcp/*.json` and `~/.config/genesis/mcp.json` (the user's entry wins by name, `disabled` drops one); their tools join the full tool set as `mcp__<server>__<tool>` (small models keep the short script), each classified by the tier the config declares (`mcp.read/write/network/system/never` in the default policy, per tool when listed) and shown on the cards as "use your <server> tool: <tool>" in the maker, Babel and the phone app. `GET /api/mcp` and `POST /api/mcp/reload` back a Tools section in Settings. The shipped `system` server (`genesis-mcp-system`, 120 lines of Python, no framework) gives the maker status, network, updates, Flathub search and install/remove, printers, processes and day/night: the "installs software and reconfigures the machine" line of the brief, with installing at the system tier. Unit tests drive a fake Python MCP server through the handshake and a call, and the config merge. Servers run unsandboxed as the user, by design (SECURITY.md). | done |
| 88 | 2026-09-16 | A chat surface, the table stakes the brief's survey listed (history, search, documents): `/chat` in the Genesis window and "Chat with Genesis" in the menu and dock. Chats are JSON files under `~/.local/share/genesis/chats`, listed newest first and searched by every word across titles and messages (`GET /api/chats?q=`). Each chat has an agent session of kind `chat`: an assistant prompt instead of the maker's, a read-only tool set (files, the index, a new `read_document` tool that runs `genesis-index extract` for PDF, Office and OpenDocument, the browser) plus the user's MCP tools, a scratch folder instead of a project, and no scaffold nudge; after a restart the stored messages are replayed into a fresh session. An attached image goes to the vision model directly. The page shows tool use as chips and permission requests as the same cards as the maker; hold-to-talk reuses the transcription endpoint. German and Slovenian from the first day. | done |
| 89 | 2026-09-16 | The file index searches by meaning as well as by words. `genesis-index` embeds every chunk with the pack's `embed` model through the router (`/v1/embeddings`, batches of 16, a few hundred chunks per timer run so it never blocks, the rest on the next run), keeps the vectors in SQLite next to the FTS5 table, and fuses the two rankings by reciprocal rank so a hit of either kind can win; each hit says `by: words`, `meaning` or both. Without an embed model the search is what it was. numpy does the dot products (in the image now), with a pure-Python fallback. Verified with a fake embedding service: "groceries" finds the shopping list that never says the word. | done |
| 90 | 2026-09-16 | The plan card, rule 2 of the design plan, was the most visible gap of the ten rules: before a job runs in Ask or Trusted mode, the maker shows what it will touch (the project folder, the network, installing software, files outside the project) as chips and the intended steps, with Proceed, Edit the request and I'll do it myself. `POST /api/sessions/{id}/plan` makes one short model call without tools for the steps and the flags; the chips are also derived from the words of the request, so a small model that answers no usable JSON still yields an honest card. Hands-off skips it; a tick box turns it off. | done |
| 91 | 2026-09-16 | The Ollama-compatible shim the brief asked for: `genesis-ollama`, a 200-line Python service on 127.0.0.1:11434 that answers `/api/tags`, `/api/version`, `/api/show`, `/api/generate` and `/api/chat` (streaming NDJSON the way Ollama does, tool calls relayed), `/api/embeddings` and `/api/embed`, and passes `/v1/*` through to llama-swap. Model names are the Genesis roles; `name:latest` is accepted. Verified against a fake OpenAI-style router: tags, a streamed generate, a non-streamed chat, embeddings, pass-through. Localhost only; a system unit like the router's. | done |
| 92 | 2026-09-16 | A ten-make evaluation in CI (`eval.yml`, weekly and on demand): the latest x86 release boots under KVM, the wizard's own API installs the tiny pack, and `prototype/eval-makes.py` runs ten small makes through the maker's API on the 2B model (Trusted mode, permission prompts allowed, 15 minutes each), scoring a make as passed when the session ends done with at least one file written. The rows (files written, turns, tool errors, preview, time, the model's last sentence) land in the job summary and an artifact. It is a measure, not a gate: the number is what the compact script and the small models are judged by from now on. | done |
\n| 93 | 2026-09-16 | Decision 85, corrected by its own diagnostic: Aurora does carry `growpart` (it landed in `/usr/sbin` and `/usr/bin` both), what it lacks is bootc's `bootc-generic-growpart.service` itself. Genesis now ships `genesis-growpart` (grow the partition with growpart, then btrfs/xfs/ext4 resize) and a unit with the same conditions as bootc's that steps aside when bootc's unit exists, so both bases grow into a resized disk on boot. The self-check accepts either unit. | done |\n
| 94 | 2026-09-16 | Routing by task, the last piece of "several models" from the brief: a maker session takes the pack's `code` model, a chat takes `chat`, the vision path takes the multimodal `fast` one, and every kind falls back to the always-loaded small model when the pack has no bigger one (so nothing changes on the tiny and cpu packs). A model named on the command line other than `auto` is used as given. The router already keeps the small models hot and swaps the big ones, so a question in chat no longer loads the coder. | done |
| 95 | 2026-09-16 | A reranker on search: packs with a `rerank` model (gpu-24 ships bge-reranker-v2-m3) get it rendered into the router config with `--reranking`, and `genesis-index` sends the fused word-and-meaning candidates (three times the asked number) to `/v1/rerank`, taking the reranker's order when it answers and the fused order otherwise; hits then say `by: words+meaning+rerank`. Verified against a fake endpoint whose scores overrule the fused order. | done |

| 96 | 2026-09-16 | OCR without an OCR model: the pack's multimodal small model reads text off pixels. `read_document` on a photo, or on a PDF that yields no text (a scan), renders up to four pages with pdftoppm and asks the vision model to transcribe each exactly; the result is marked as read from the image. Covers the common case the brief's OCR line was for; a dedicated OCR model can still come as a pack role later. | done |
| 97 | 2026-09-16 | Prompt templates in chat, the last table-stakes item from the brief's survey: seven shipped (`/usr/share/genesis/chat-templates.json`: summarise, explain, reply, translate to German and Slovenian, checklist from notes, what changed on this computer) plus the user's own, saved from the composer and kept in `~/.local/share/genesis/chat-templates.json`; `GET/POST /api/chat-templates`, delete per id. | done |

| 98 | 2026-09-16 | The system server grows into the administrator the brief described: find network printers and set one up through CUPS (system tier), diagnose wifi (connection, gateway, internet by address, DNS, signal, in plain words), show or set the power profile, Bluetooth and audio devices. Sixteen tools now. | done |

| 99 | 2026-09-16 | Licensing tiers enforced rather than merely declared: every model in a pack must carry a licence and a tier (`main`, `restricted`, `excluded`); the publish job refuses a pack with an excluded model and marks a pack with restricted ones (`restricted: true` in the signed artifact); the wizard shows a restricted-licence tag on such models. All shipped packs are main today, so nothing changes for users. | done |

| 100 | 2026-09-16 | Speculative decoding as a pack role: a pack may ship a `draft` model next to its coder; on a real GPU the router config then runs llama-server with `-md` and a draft window of 16, which roughly doubles generation on dense 27B coders. Off on CPU-only machines, where a draft slows things down, and never on MoE models (they do not get a draft). The gpu-24 and gpu-48 packs can add a draft in a later data change. | done |
| 101 | 2026-09-16 | First numbers from the evaluation (three makes, tiny pack, 4 vCPU runner): 1 of 3. The word-count tool passed in 110 s with a preview; the checklist app hit the 40-turn limit after 34 file writes and no run (the 2B model looping on writing); the pomodoro timer timed out at 900 s after four calls (the CPU runner is slow, not the model wrong). Two changes: the agent tells a model that has changed files eight times without running anything to run and finish (once per job), and the evaluation waits 25 minutes per make and records the tool sequence and the tool errors of each make so a failure can be read from the artifact. The ten-make number is now the yardstick; the next weekly run says whether the nudge helped. | done |
| 102 | 2026-09-16 | Localisation complete for every surface a person sees on day one: the first-run wizard's remaining 30 strings in German and Slovenian, and Babel's welcome page in both languages, picked by the editor's language. JavaScript and TypeScript in Babel need no extra server: Code-OSS bundles its TypeScript language features, so the roadmap line is closed as already true. | done |
| 103 | 2026-09-16 | Verified live on the first image that carries the day's work (0.2.153 in the arm64 VM): 89 self-check lines ok, the chat page answering "is an update waiting, which apps are installed" through two system tools on the 2B model, the plan card before a make, Settings listing the system server, the Ollama door answering `/api/tags`, both grow units in place. Captures under docs/screens/brief (taken with Spectacle inside the guest: QEMU's screendump shears at this window width). Found in the Activity log: an MCP call was classified as a shell command, because the classifier ran its command rules on any intent with a command text; command rules now apply to shells only and a test pins the declared tier of MCP tools. The card and Settings sections also scroll themselves into view now. | done |
| 104 | 2026-09-16 | The canary's new journal line showed the grow unit finishing without growing anything, and running the script by hand on the arm64 VM gave the reason: `lsblk -o PARTN` pads the number with spaces and growpart refuses " 3". Trimmed; the script now reports "nothing to grow" on an already-grown disk. Lesson kept: a oneshot that exits 0 on its own errors needs its output in the canary, not just its status. | done |
| 105 | 2026-09-16 | Issue #2 closed with two templates: `python-api`, a JSON API on the standard library (routes table, data file, CORS, tests that call the routes directly; verified with pytest and a live server), and `gtk-app`, a real desktop window with GTK 4 through python3-gobject, both already in the image so it runs with no install (header bar, entry, list, saved items; started in the VM's session to prove it opens). The compact script names both. Issue #5 (screen-reader pass) closed on decision 84; #1 updated with the chat page and Babel's welcome files. #3 and #6 stay open: they need hardware we do not have. | done |
| 106 | 2026-09-16 | The first full evaluation, the baseline before any of the day's agent changes (release 0.2.153, tiny pack, 4 vCPU runner): 4 of 10. Passed: word count (30 s), JSON flag, temperature converter (14 turns), todo CLI. Failed, and why: the checklist app edited the same file 37 times and never ran it; the quotes page rewrote genesis.json until the manifest no longer parsed (`expected struct RunSpec`); the photo renamer looked for its entry file at the wrong path 40 turns long; the pomodoro edit gave an empty path; club and dice died in 5 s with no tool call (a session-level error the results did not capture). Four guards follow, each aimed at one of those: a hard stop after 16 changes without a run (the nudge at 8 stays), a plain error for a tool call without a path, genesis.json refused for editing with the entry file's full path in the message, and the scaffold result naming the entry file by its full path. The evaluation now records session errors too. The VM runs 0.2.157: 90 self-check lines, 16 system tools, 7 templates, the Ollama door listing `fast` and `embed`. | done |
| 107 | 2026-09-16 | The wow plan (docs/genesis-wow-plan.html): the brief's list is closed, so the next plan is written against what people would notice. Honest state first (rules 4 and 8 partial, 4 of 10 on the tiny pack, no real hardware, a slow first ten minutes, English-only voice, the phone app by cable), eight improvements to what exists, ten moments ranked by impact over effort (show me then do it, the morning card, a wake word, fix my computer, make it while I sleep, one picture, phone and desktop as one, time machine, Babel inline, the sixty-second demo), phases G to K with proof for each, four numbers that mean done, and the gate that stays in front of everything: a real GPU machine. | accepted |
| 108 | 2026-09-16 | Phase G of the wow plan. The first ten minutes: the wizard downloads the small model first and brings the router up the moment it is on disk (`on_file` hook in the downloader), so the first make no longer waits for the embedder, the voices and the big models; the evaluation now prints how many seconds after the choice the small model served. The maker on small models: template `hints` repeated in the scaffold result (what to change first), a no-op guard for edits, and a compact script that says write the entry file once and fix at most three times. Rule 4 finished: `genesis-mode` (show, cycle, ask, trusted, hands-off) with Meta+Shift+M, the status widget shows the mode with a Cycle button, and Settings keeps always/never command rules in `~/.config/genesis/policy.toml`, which permd already reads and agentd reloads on change. Rule 8 without the wake word: Esc stops a spoken reply, a spoken install/delete/send is shown and needs Enter. Voices: Piper's German and Slovenian voices in every pack as optional files, chosen by the desktop's language. | done |
| 109 | 2026-09-16 | Phase H of the wow plan. Fix my computer: four chat templates (Wi-Fi, printer, battery, slow), two fixing tools in the system server (`wifi_reconnect`, `print_test_page`, write tier) and one sentence in the chat prompt: diagnose with the reading tools, say what is wrong, then call the one fix you would try first, so the permission card is the button. The morning card: a once-a-day notice built from local facts (staged update, yesterday's makes not installed, Wi-Fi signal, battery), dismissed with Got it, the same card on the phone through the notices endpoint. The time machine: `GET /api/history/{id}/changes` diffs each file's pre-image against what is there now (a capped line diff with folded context, in `timeline.rs`), `restore-file` puts one file back; Settings shows it under Changes. Settings regrouped into Machine, Genesis, Phone and History. | done |
| 110 | 2026-09-16 | Phase I of the wow plan. Phone and desktop as one: sessions were already shared, so the missing pieces were the two directions of hand-off: `genesis-phone send` and a To phone button in the palette (KDE Connect share of text or a file), and a share target in the Android app that posts to a new `/v1/share` on the companion daemon, which opens a link in the browser or drops text into the palette. Make it while I sleep: a queue (`~/.local/state/genesis/queue.json`) with a run time, Start now and a Tonight button in the maker; a runner thread in agentd starts the next item when it is due, the machine is on mains and nothing else runs, in Trusted mode with every prompt answered not now, thirty minutes at most per item, the outcome recorded and reported on the morning card. The APK on releases: `android.yml` builds and attaches a signed APK when the four signing secrets exist and an unsigned debug build otherwise; the keystore stays outside the repository, adding the secrets is the founder's step. | done |
| 111 | 2026-09-16 | Phase J of the wow plan, minus the wake word. Say it, it happens: `Meta+Shift+V` opens the palette listening at once and it sends when you pause (an energy gate: nothing is sent while the room is silent, fifteen seconds at most), which is the honest form of a wake word until a model trained on the word "Genesis" exists; openWakeWord's stock phrases are not ours and were not used. Babel inline: `Ctrl+I` on a selection or line, one instruction, the rewrite lands in place through a chat-kind session with no tools and Ctrl+Z drops it; Explain this error explains the diagnostic under the cursor. The sixty-second demo: "Show me Genesis" in the app menu runs `genesis-demo` on the real maker (badge, a make, a card, install, ask about the screen, undo) narrated by notifications, and docs/demo-script.md is the recording script for the launch video. | done |
| 112 | 2026-09-16 | Verified by the canary on release 0.2.164: the x86 root grows into a resized disk on first boot (22 GB to 38 GB, `growpart` CHANGED, the filesystem resized through /var, and "nothing to grow" on the next boot). Three findings in a row from one canary line: a missing unit, a padded number, a read-only mount. The README's claim is true on every flavour now. | done |
| 113 | 2026-09-16 | Second full evaluation, on release 0.2.164 (the guards, template hints, the tighter compact script and the early router): **8 of 10**, up from 4 of 10 on the baseline the same afternoon. Checklist, pomodoro, word count, photo renamer, club site, JSON flag, temperature converter and quotes page all passed with a preview, most in under six minutes on the 4 vCPU runner. The two misses: the dice roller kept rewriting its file and was stopped by the new hard stop after sixteen changes (the guard worked; the make still counts as failed), and the todo CLI crashed in the harness itself: answering a permission prompt that had already timed out returned 404, which the script now ignores. The first-ten-minutes change measured: the small model was serving 81 seconds after the pack was chosen, the whole pack 103 seconds later on the runner's line, where the old flow waited for everything. Phase G's two numbers, 7 of 10 and under ten minutes, are met on the first run; the weekly evaluation says whether they hold. | done |
| 114 | 2026-09-16 | Verified on 0.2.167 in the VM (93 self-check lines): share to the desktop from the phone app end to end: text shared to the Genesis app on the emulator opened the maker on the desktop with that text as the request, through the companion daemon's new route. Two things on the way: the app now handles an intent that reaches an already running activity, and the daemon reaps the desktop programs it starts. A second full evaluation, on 0.2.163, also scored 8 of 10, so the number holds across two runs. | done |
| 115 | 2026-09-16 | The sixty-second demo run on the VM (0.2.167): all six narrated steps went through on the real maker with the 2B model, the checklist app was made and installed, Settings opened on the undo history at the end, the final notification on screen. The demo entry and the talk key are in the image and the self-check. The day's plan, phases G to J, is built and verified; K waits for a GPU machine. | done |
| 116 | 2026-09-17 | Past 8 of 10: the one make that failed in both evaluation runs, the dice roller, rewrote a single file until the hard stop. Two changes aimed at exactly that: the fourth rewrite of the same file with no run in between is refused with the instruction to run it and read the output, and a run that ends without an error after changes tells the model it has reached the finish line and should summarise. The harness fix from decision 113 covers the other miss. The next evaluation run is the proof. | built |
| 117 | 2026-09-17 | An audit the day before the first install on real hardware, every finding checked against the code or the VM before it was believed. Two were serious. (1) The model service could not open the GPU: `DeviceAllow=/dev/dri rw` names a directory, systemd ignores it, and with `DevicePolicy=closed` the render node was shut, so every model would have run on the CPU without a word; confirmed in the VM (the same open fails with the old line, succeeds with `char-drm`). Now device classes (`char-drm`, `char-kfd`, `char-nvidia*`), and the self-check opens the render node as the service user inside the sandbox. (2) The installer ISO shipped the test account (genesis/genesis, wheel) and the kernel argument that starts sshd, because ISO and test disks shared one builder config. The ISO has its own config now with neither; Anaconda makes creating an account mandatory; the self-check fails when an installed machine still has the test password. Also: the big models leave `-ngl` unset on a GPU so llama.cpp's fit places what the free memory allows (an 8 GB card could not hold the resident small models plus a 6 GB coder with every layer forced onto it); flash attention `auto`; Intel Arc memory read from the PCI region; AMD no longer needs ROCm's `/dev/kfd` to count as ready; the probe waits for udev and runs again before the wizard; the first-run window is maximized, not fullscreen, with an offline notice, so Wi-Fi is reachable; the install checklist corrected (one ISO, Secure Boot key, NVIDIA by `bootc switch`). Left open: every machine is named `genesis` by a kernel argument; memory budgeting still counts only the largest model. | done |
| 118 | 2026-09-17 | The second half of the audit: yesterday's new code, each finding checked before it was fixed. (1) The agent could have approved its own permission cards: a shell with network access shares the loopback, a bare GET of the maker page returned the API token, and with the token it could answer prompts, change the mode or add an always rule. The token now reaches a page only through the Genesis window (a request interceptor reads it from the runtime directory, which the sandbox cannot see); a bare GET gets a page that can change nothing, and GETs that return secrets (the phone pairing payload, the backup, the rules) need the token. (2) The policy files that decide the agent's own permissions (`policy.toml`, `rules.json`, `mcp.json`) are in the secrets tier, and an overlay from the home folder can no longer loosen the never and secrets tiers, replace a built-in rule or remap tool tiers (test added). (3) A bad always/never pattern could have stopped the daemon from ever starting: patterns are validated, a rule that does not load is rolled back, and the daemon falls back to the image's policy when the user's overlay is broken. (4) A document over 60 KB with a multi-byte character at the cut panicked and froze the session: truncation respects character boundaries. (5) An unknown MCP tier string failed open to read; it now means system. (6) `--all` as a Flatpak id would have removed every app behind one card: ids are validated and passed after `--`. (7) Once a conversation has read the user's documents, going online asks first in every mode (test added), so a hostile PDF cannot send data out silently. (8) Text shared from the phone is a draft in the maker, never run on its own. (9) The demo says honestly that it answers the cards for you, and answers the install card too. Left for later, none urgent: the Ollama door's streamed tool calls and HTTP version, the queue's missing-folder retry and session cleanup, OCR timeouts, stale vectors after a folder is removed, the write counters counting failed writes. | done |
| 119 | 2026-09-17 | Third evaluation, on 0.2.169 with the refuse-the-fourth-rewrite guard: 8 of 10 again, the small model serving 59 s after the pack choice, and seven of the eight passes under six minutes. The guard did not help: the dice roller and the todo CLI kept calling write_file after being told the write was refused; a 2B model does not act on a refusal. So the decision is taken away instead: on the third rewrite of one file with no run in between, Genesis runs the program itself (the template's dev command) and puts the output in the tool result, with the instruction to summarise if it is clean or to fix only what it shows. The next run says whether that moves the number. | built |
| 120 | 2026-09-17 | A review of the whole thing (docs/genesis-review.html), two independent reads checked against the code. Good: the plain-verb cards, the plan card, inspectable undo, honesty about the hardware, the tested broker and real CI. Bad: no way to stop a job; the guide promises buttons and names the screens do not have; the palette turns every question into a make; two design systems; 13 desktop entries and 6 shortcuts; jargon on screen; paths typed by hand; every push moves the tag users update to, with CI as the only test bed; 160 KB of untested one-line JavaScript; one token on loopback as the security model. Missing: model management, retry and feedback, off switches, update notes, what is stored and how to delete it, project lifecycle, file pickers, release pruning, a staging channel. New: ten ideas that build on what exists (make a tool for this file, undo the last ten minutes in the panel, pull-the-cable proof, terminal rescue, clipboard transforms and five more). Order: trust the basics, stop shipping blind, first-week gaps, then the new ideas; Unix sockets as the structural security change. | accepted |
| 121 | 2026-09-17 | The review's bad list, first batch. Stop: a flag on the session ends the loop at its next checkpoint, the model wait runs on a helper thread so Stop answers within a quarter of a second, a running command is killed, a waiting card ends; `POST /api/sessions/{id}/stop`, a Stop button in the maker and in chat. The palette is one door for two things: a request to make opens the maker, a question goes to chat where it is kept (a verb and noun test in three languages, Ctrl+Enter forces the maker, Make it in chat hands a text over). The guide made true to the screens: the cards have two buttons, the modes are called Ask, Trusted and Hands-off, updates apply at the next restart, the screen-reader claim says what was actually checked. The wizard no longer offers cloud keys that nothing can take, and points to Settings, Activity for the log. Plain words on screen: small model / coder model instead of role ids, working / waiting for you instead of raw states, steps read Writing app.py and Running it with the raw call in the tooltip, and the maker's undefined colour token is defined. | done |
| 122 | 2026-09-17 | The review's bad list, second batch: stop shipping blind. GET routes are closed by default: a short explicit list is open (the pages, and health, system and sessions for the dock widget), everything else needs the token, with a test that names fourteen routes that must stay closed. CI now parses every shell and Python tool, every JSON file, the Babel extension and the JavaScript inside the five HTML pages before anything is built. llama.cpp's binary is checked against its sha256 like the other downloads (the digest matches the one GitHub records for the asset). The canary runs every night instead of weekly and opens an issue when it is red and closes it when green, because a red canary holds the stable tag and told nobody. `prune-releases.yml` lists and, only when run by hand with dry_run false, deletes rolling releases beyond the newest fifteen (122 releases at about 15 GB each had piled up). One set of design tokens across the maker, chat and Settings, the three unstyled Settings classes styled, the chat microphone's on state styled. Chat and Settings have their own icons, one redundant menu entry is hidden. Not done yet: a testing tag promoted to stable, the JavaScript moved out of the HTML files, Unix sockets instead of loopback TCP. | done |
| 123 | 2026-09-17 | The review's bad list, third batch: the small frictions. A native file and folder dialog (`POST /api/pick`, KDE's own, in the user's session) behind Choose… buttons in Settings (folders to search, backup to restore) and chat (attach), so no path has to be typed. The undo history lists what was asked instead of a job id (the request is noted in the transaction). Problems in the maker are said on the page, not in a system alert box. Recent items are reachable by keyboard. | done |
| 124 | 2026-09-17 | Fourth evaluation, on 0.2.174 with the run-it-for-the-model change: 6 of 10, worse than the 8 before it, and the failures say why. The word counter was edited 36 times with a clean run in front of the model after every third edit, and ran into the turn limit: showing a 2B model a clean run does not make it stop, and the auto-run reset the hard stop that would have. The photo renamer overflowed the 16k context (a 400 from the model service). Two makes died because the model service itself went away (EOF, then connection refused): ten makes leave ten sessions with preview servers and a headless browser behind, and the memory ran out. Three changes. A clean run after a third rewrite now ends the job: Genesis says it is made and runs, commits, and stops; the hard stop keeps counting. Old tool output and file contents are trimmed once the conversation passes about 9k tokens, the last eight messages stay whole. Finished sessions beyond the newest three give up their agent (previews, browser) when a new session starts; their steps stay readable. The dice roller, which failed in all three earlier runs, passed in 40 s here. | built |
| 125 | 2026-09-17 | The evaluation of 0.2.178 never reached a make, and that was the finding: the pack download ended twenty-two minutes in with curl's `HTTP/2 stream was not closed cleanly`, which curl does not retry, the wizard gave up, and because the early start waited for both files of the small model (the model and its vision projector) no model came up at all. A person on a flaky line would have sat in front of a failed wizard. Downloads now use HTTP/1.1 with `--retry-all-errors` and resume, the wizard tries a file three times before it reports a problem, and the router starts as soon as the model file itself is on disk; the projector joins at the final render. | done |
| 126 | 2026-09-17 | Evaluation of 0.2.180: **9 of 10**, the best so far (4, 8, 8, 8, 6, 9 over two days), and every pass under four minutes on the 4 vCPU runner. The small model served 23 seconds after the pack was chosen. What moved it: a clean run after a third rewrite ends the job, old output is trimmed to fit the context, finished sessions free their previews, and the download survives a dropped connection. The one miss is the same disease in a new place: the word counter was written once, ran cleanly, and the model then called preview_start 36 times until the turn limit. The cure is the same: the same run a third time in a row, clean, after changes, ends the job. The lesson of the week for small models: do not tell them they are done, decide it. | done |
| 127 | 2026-09-18 | Two channels instead of one. Every build pushes `:testing` (and its versioned tag); `:stable`, which installed machines follow, moves only when the nightly canary has upgraded a week-old machine to `:testing` and found it good, by retagging the same digest for all three flavours (the cosign signature is on the digest, so it stays valid). The old gate (hold stable while the last canary is red) becomes unnecessary and is gone. A push to main no longer reaches anyone's machine unseen; the cost is that a fix takes until the next night to reach users, or a manual canary run with promote=true. The dev VM follows `:testing-arm64`. The rolling releases were pruned to the newest twenty (109 deleted, about 1.6 TB). | done |
| 128 | 2026-09-18 | The audit leftovers, closed with tests where a test was possible. A Python suite (`tests/python`, run by CI) for the three tools that had none, and it found four real defects the moment it ran: the Ollama shim answered HTTP/1.0 while sending chunked bodies (a Go client would have seen chunk headers inside the JSON), it turned one streamed tool call into several calls with fragments of JSON as arguments, and it had no Host or Origin check, so a web page could drive it; the file index kept the vectors of a folder the user had opted out of, and would have raised on every search after a pack change with a different embedding size (now vectors carry their size and stale ones are re-made). Four agent-loop tests cover the guards from decisions 116 to 126 (the forced run and finish, the repeated run, Stop killing a running command, context trimming). Also: the MCP call timeout is 1900 s, longer than the 1800 s an install may take, so a server is no longer killed mid-install; failed and unchanged writes no longer count towards the write guards; OCR kills a render after two minutes and always removes its temp directory; a queued make whose folder is gone is reported once and dropped instead of retried every minute, and one over its half hour is stopped rather than left running; `kdialog` is in the image, without which the Choose… buttons could not open (found on the VM); the hostname is `/etc/hostname` again, not a kernel argument, so a name chosen in the installer or with hostnamectl finally takes. | done |
| 129 | 2026-09-18 | The Stop test failed in CI and it was right: Stop killed the shell but not what the shell had started, and the dead shell's still-open pipe then blocked the read, so `sleep 30` outlived Stop by its full thirty seconds. In a real make that means a dev server or a package install keeps running after Stop and after the 300 s timeout. Commands now run in their own process group and the whole group is killed (TERM, then KILL); the pipes are drained on their own threads while the command runs, which also fixes the audit's note that output larger than a pipe buffer stalls a command until the timeout. Two tests: a shell whose child outlives it ends within ten seconds, and a command printing 400 KB does not hit the timeout. | done |
| 130 | 2026-09-18 | Docker's disk image was reset (it had grown to 239 GB and its daemon had not answered since the disk filled), so the local build loop works again, and the first thing it did was find two things CI could not. An amd64 build on this Mac dies unpacking piper with `tar: Function not implemented`, which is the x86 emulation layer, not the image: the local recipe is now the arm64 flavour, which builds natively (`just image-local`), and CI keeps building amd64 on real amd64. The hostname self-check failed on a correct image because it grepped the whole kargs file including the comment that explains the change; it now reads only the `kargs` line. Verified in the built image: `kdialog` present, `/etc/hostname` is `genesis` and changeable, `kargs = []`, 96 self-check lines green, none failing. Reading `/etc/hostname` needed a `COPY --from` out of the image, because Docker bind-mounts that path at run time; `just image-file` does it. | done |
| 131 | 2026-09-18 | The process-group kill of decision 129 worked on macOS and not on Linux, and the restored local loop said why in two minutes: it shelled out to `/bin/kill`, which does not exist on a minimal Linux (there `kill` is only a shell builtin), so the group survived and its open pipe blocked the read for the full command. It now calls the kill syscall directly (libc, TERM then KILL), verified by running the two Stop tests inside a Linux container on this Mac, where they had failed. The lesson is the one the engineering review made: CI as the only test bed hides platform bugs for ninety minutes at a time. | done |
| 132 | 2026-09-18 | The review page (docs/genesis-review.html) updated with what has been done since it was written, item by item, and what has not. Done: Stop, the guide made true to the screens, the palette as one door for asking and making, the testing and stable channels, release pruning, deny-by-default GET routes, local builds. Partly: one design system (three of five pages), the doors and the jargon, the pickers (built, kdialog verified in the image, not yet used on a real image), the inline JavaScript (parsed by CI, still untested), the security structure (tightened, but still loopback TCP). Open: the whole first-week list apart from the pickers, and all ten wow ideas, none started. | accepted |
| 133 | 2026-09-18 | The first-week list, the part a daily user meets. What is kept about you: Settings now lists every place Genesis stores something (chats, the search index, the activity record, undo snapshots, the queue) with its real path and size, a delete button each, and the plain statement that voice audio is never kept; snapshots delete only what is older than a week, so today's Undo still works. When Genesis speaks first: the proactive cards and the morning card each have a switch, and nothing else speaks unasked. Updates: the release notes ship in the image and Settings shows What changed. Models: the size on disk and a button that reopens the model setup for a different pack. Things you made: a small More menu on each card for show in files, rename, take out of the app menu, and delete to the trash, never straight to nothing. Chat: copy on every answer and Try again on the last one. Onboarding: a card on the first visit with the six shortcuts, dismissed for good. | done |
