# Fresh-disk run, 2026-09-15

The arm64 release 0.1.102 downloaded from GitHub, checksums verified, booted on a fresh disk in the VM (8 GB RAM, no GPU), and used as a new user: first run recommends the tiny pack (1.9 GB), downloads it, the wizard ends with "a checklist app that saves to a file", and the maker builds it with the 2B model on CPU. A second make from the launcher ("a pomodoro timer with a bell") finished in about four minutes.

Then the same machine switched to the `stable` channel and rebooted into 0.2.107 with everything intact (04).
