# Genesis: updates are fetched and staged in the background and applied at the next reboot.
# bootc's default would also reboot the machine; a desktop must never do that on its own.
[Service]
ExecStart=
ExecStart=/usr/bin/bootc upgrade --quiet
ExecStartPost=/usr/bin/systemctl start --no-block genesis-bootc-status.service
