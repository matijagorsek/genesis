# Kernel arguments shipped with the Genesis image (bootc kargs.d).
# The hostname is NOT set here any more: systemd.hostname= overrode /etc/hostname, so every machine was
# called "genesis" and a name chosen in the installer or with hostnamectl never took (audit, 17 Sep).
# The image ships /etc/hostname instead, which the installer and hostnamectl can both replace.
kargs = []
